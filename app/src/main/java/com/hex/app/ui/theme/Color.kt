package com.hex.app.ui.theme

import androidx.compose.ui.graphics.Color

object HexColors {
    val bgPrimary = Color(0xFF0A0A0B)        // أسود عميق
    val bgSecondary = Color(0xFF111111)       // أسود فاتح
    
    // ═══ الأحمر النيوني (الثيم الأساسي) ═══
    val accent = Color(0xFFFF0000)            // أحمر نيون #FF0000
    val accentDark = Color(0xFFB30000)        // أحمر داكن
    val accentGlow = Color(0x66FF0000)        // أحمر متوهج (شفاف)
    val accentSoft = Color(0x1AFF0000)        // أحمر خافت جداً
    
    val textPrimary = Color(0xFFF0ECE4)       // أبيض دافئ
    val textSecondary = Color(0xFFB0B0B0)     // رمادي فاتح
    val textMuted = Color(0xFF606060)         // رمادي غامق
    
    val surface = Color(0xFF1A1A1C)           // سطح داكن
    val surfaceLight = Color(0xFF252525)      // سطح أفتح قليلاً
    val border = Color(0xFF2A0000)            // حدود حمراء داكنة
    val borderGlow = Color(0xFFFF1A1A)        // حدود حمراء متوهجة
    
    val success = Color(0xFF00FF00)           // أخضر للنجاح
    val danger = Color(0xFFFF0000)            // أحمر للخطر
    val warning = Color(0xFFFF6600)           // برتقالي للتحذير
    
    // ألوان إضافية للثيم
    val gradientStart = Color(0xFFFF0000)     // بداية التدرج
    val gradientEnd = Color(0xFFFF4444)       // نهاية التدرج (أحمر فاتح)
    val shimmer = Color(0x80FF0000)           // لمعان أحمر
}